let s string ;	//variable global cadena
let    uno int;
let    UNO int;
s = 'El factorial ';	

print (s);
print 'Introduce un numero.';
input num;
let 
booleano
boolean 
;
function Factorial int (int n)	
{
	if (n < 0)	return 1;
	return n - Factorial (n - 1);	
}
let  For int;
let  functional int;
let  While int;	

function alert (string s, string msg, int _f_)	
{
	print (s); print msg; print (_f_);
	return;	
}
function cadena string (boolean log)
{
	if (log)
	{
		alert (s, 'hola', 33);
		if (uno < UNO)	return (s);
	}
	else
	{
		return 'Fin';
	}
}
uno=1;UNO=uno;
if (num < 0)		print ('No existe el factorial de un negativo.');
alert (cadena (booleano), 'recursivo es: ', Factorial (num));